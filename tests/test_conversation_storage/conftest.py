"""
Test fixtures for conversation storage tests.

This provides the necessary mocking infrastructure to test conversation
storage without requiring real API keys or external dependencies.
"""

# For now, conversation storage tests are isolated and don't need MCP mocking
# Baseline tests that need mocking should import from test_mcp.conftest directly
